# Core RAG dependencies
pymupdf
langchain
langchain-text-splitters
sentence-transformers>=2.7.0     # BAAI/bge-base-en-v1.5 support
chromadb>=0.5.0
python-dotenv
requests

# Utilities
tqdm
duckdb

# Groq SDK (optional, we use raw REST but this is handy for debugging)
groq

# Evaluation helpers
# numpy is already a transitive dep of sentence-transformers
numpy

# If you need FAISS for local ANN search (optional, ChromaDB covers it)
# faiss-cpu
rank_bm25